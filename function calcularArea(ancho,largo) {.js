function calcularArea(ancho,largo) {
    const area = ancho * area;
    return area;
}

const  calcuarPerimetro = function(ancho, largo) {
    const ancho = 2* ( ancho + largo );
    return ancho; 
}

const anchorectanulo= 4
const largorectanglo= 5

const anchoresultado= calcularArea ( anchorectangulo, largorectangulo );

console.log(`El área de un rectángulo de ${anchoRectangulo}x${altoRectangulo} es ${areaResultante}.`);

const perimetroResultante = calcularPerimetro(anchoRectangulo, altoRectangulo);
console.log(`El perímetro del mismo rectángulo es ${perimetroResultante}.`);